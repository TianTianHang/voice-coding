Native TEN VAD runtime libraries are downloaded or built into this directory.

Run `scripts/download_ten_vad.sh` or `scripts/build_ten_vad.sh` from the repository root to populate platform-specific library files.
