## 1. 状态模型与信息架构重构

- [x] 1.1 在前端定义体验状态模型（Dormant、WakeDetected、Listening、Processing、Responding、Error）并建立与 VAD/Agent 事件的映射规则。
- [x] 1.2 重构 `AssistantConsole` 主布局为“主舞台 + 理解闭环 + 回应区 + 按需展开时间线 + 低优先级兜底控制”。
- [x] 1.3 调整低频配置（如关闭行为）与手动控制的层级，确保不抢占语音主流程视觉焦点。

## 2. 在场感与激活体验实现

- [x] 2.1 为主舞台实现 Presence Orb 的多状态视觉样式（待机、唤醒确认、聆听、处理、回应、错误）。
- [x] 2.2 实现激活仪式与状态过渡动效（短时唤醒确认、处理中反馈、回应态反馈），并控制时长与节奏。
- [x] 2.3 为 `prefers-reduced-motion` 提供低动态降级方案，保证可访问性一致。

## 3. 可信反馈闭环与输出流策略

- [x] 3.1 实现 Heard/Intent/Status/Response 四类最小可信反馈区，并接入现有实时数据源。
- [x] 3.2 调整 `AgentEventStream` 展示策略为默认按需展开，保留显式展开入口。
- [x] 3.3 在 `confirm` 或 `error` 事件出现时自动提升时间线可见性，确保关键事件可立即处理。

## 4. 文案与视觉一致性收敛

- [x] 4.1 统一语音状态文案为短句现在时（Ready/Listening/Working/Responding/Error）并对齐中文界面表达。
- [x] 4.2 在 `App.css` 或等效样式层补充语音优先设计 token（语义色、层级、字体、间距、阴影）。
- [x] 4.3 校验桌面与窄屏下的布局连续性，确保语音主路径在不同尺寸下语义一致。

## 5. 测试与质量验证

- [x] 5.1 更新或新增前端测试，覆盖关键状态迁移、主舞台状态可见性、时间线自动展开与回退行为。
- [x] 5.2 运行 `pnpm test` 并修复与本次改动相关的失败项。
- [x] 5.3 运行 `pnpm build` 并修复构建或类型错误。
- [x] 5.4 记录本次执行的验证结果（测试、构建、已知阻塞）并确认变更达到可实施完成标准。
