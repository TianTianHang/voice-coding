## 1. TTS 标签解析

- [x] 1.1 在后端增加 `<tts>...</tts>` 提取和 UI 隐藏辅助逻辑，区分原始文本、展示文本和口播文本
- [x] 1.2 覆盖有效单标签、缺失标签、多标签、不完整标签、空标签、嵌套标签和大小写不匹配的 Rust 单元测试

## 2. 后端自动播报集成

- [x] 2.1 将自动 TTS 触发路径改为只使用有效单个 `<tts>` 标签内文本，缺失或无效时严格跳过
- [x] 2.2 调整最近结果、重播和去重逻辑，使其基于提取后的 TTS 文本而不是完整 result 文本
- [x] 2.3 在自动 TTS 状态或错误/跳过路径中保留可调试的跳过原因，不影响正常 UI 交互

## 3. UI 展示集成

- [x] 3.1 在 agent result 展示路径隐藏所有完整 `<tts>...</tts>` 块，保留标签外文本
- [x] 3.2 保持现有 `messageId` 增量合并行为，确保隐藏标签不会创建重复输出块或破坏流式显示
- [x] 3.3 增加前端测试覆盖单标签、多标签、流式合并后隐藏和不完整标签展示稳定性

## 4. Agent 提示词契约

- [x] 4.1 更新 agent profile 或系统提示词，要求需要口播时输出恰好一对 `<tts>...</tts>` 标签
- [x] 4.2 明确提示 `<tts>` 内只能包含短自然口语文本，不包含 Markdown、代码、diff、日志、文件路径或工具输出
- [x] 4.3 明确提示无须口播时省略 `<tts>` 标签，禁止输出多对 `<tts>` 标签

## 5. 验证

- [x] 5.1 运行 `pnpm test`
- [x] 5.2 运行 `pnpm build`
- [x] 5.3 运行 `nix develop -c cargo test`
- [x] 5.4 运行 `nix develop -c cargo clippy`
- [x] 5.5 记录所有验证结果；如有未运行或失败，记录明确原因和后续处理建议
