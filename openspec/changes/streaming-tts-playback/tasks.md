## 1. 抽象与数据结构
- [x] 新增 `tts-core` crate，定义 TTS 错误、配置、结果和引擎 trait。
- [x] 明确 TTS 输出音频的数据结构，包含 48kHz 立体声 PCM 约束。
- [x] 为流式内部产出预留进度或分片类型，但保持对外以完整结果为主。

## 2. 播放与音频输出
- [x] 新增后端音频输出模块，基于 `cpal` 创建输出流。
- [x] 实现音频缓冲与播放消费逻辑，保证音频回调轻量且稳定。
- [x] 实现输入格式到目标播放格式的适配路径。

## 3. TTS 运行时
- [x] 新增 TTS runtime，负责合成状态、播放状态和错误状态管理。
- [x] 新增合成完成后再播放的控制流程。
- [x] 新增播放取消与失败后的资源清理逻辑。

## 4. 录音联动
- [x] 在播放开始时暂停当前录音/VAD 会话。
- [x] 在播放结束或失败时恢复录音/VAD 会话。
- [x] 确保播放期间不会继续采集麦克风音频。

## 5. Tauri 命令与事件
- [x] 新增准备、查询状态、合成、播放相关命令。
- [x] 新增 TTS 状态事件，便于前端展示播放阶段。
- [x] 与现有后端事件风格保持一致。

## 6. 测试与验证
- [x] 为状态机、暂停/恢复流程、错误路径编写 Rust 单测。
- [x] 为音频格式与播放边界添加测试。
- [x] 运行 `cargo test`、`cargo clippy` 和 `pnpm tauri build` 验证后端修改。
